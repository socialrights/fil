
// path
$basepath ?= '../../node_modules/bootstrap-4-stylus/'

//  colors
$white ?= #fff
$gray-100 ?= #f8f9fa
$gray-200 ?= #e9ecef
$gray-300 ?= #dee2e6
$gray-400 ?= #ced4da
$gray-500 ?= #adb5bd
$gray-600 ?= #868e96
$gray-700 ?= #495057
$gray-800 ?= #343a40
$gray-900 ?= #212529
$black ?= #000

$primary ?= #734E30
$primaryHov ?= lightness($primary, 20%)
$primaryBtnHov ?= $primaryHov
$secondary ?= #246EB9
$secondaryHov ?= lightness($secondary, 20%)
$secondaryBtnHov ?= $secondaryHov
$success ?= #659B5E
$successHov ?= lightness($success, 20%)
$successBtnHov ?= $successHov
$info ?= #246EB9
$infoHov ?= lightness($info, 20%)
$infoBtnHov ?= $infoHov
$warning ?= #F7B538
$warningHov ?= lightness($warning, 20%)
$warningBtnHov ?= $warningHov
$danger ?= #EF3054
$dangerHov ?= lightness($danger, 20%)
$dangerBtnHov ?= $dangerHov
$light ?= $gray-100
$lightHov ?= lightness($light, 20%)
$lightBtnHov ?= $lightHov
$dark ?= $gray-800
$darkHov ?= lightness($dark, 20%)
$darkBtnHov ?= $darkHov

$bodyColor ?= $white
$bodyBackground ?= $white

// col includes
$col-include ?= true
$col-sm-include ?= true
$col-md-include ?= true
$col-lg-include ?= true
$col-xl-include ?= true

// flex includes
$flex-include ?= true
$flex-sm-include ?= true
$flex-md-include ?= true
$flex-lg-include ?= true
$flex-xl-include ?= true

// grid breakpoints
$grid-sm ?= 576px
$grid-md ?= 768px
$grid-lg ?= 992px
$grid-xl ?= 1200px

// container includes
$container-sm-include ?= true
$container-md-include ?= true
$container-lg-include ?= true
$container-xl-include ?= true

// container-max-size
$container-sm ?= 540px
$container-md ?= 720px
$container-lg ?= 960px
$container-xl ?= 1140px

$line-height-lg ?= 2
$line-height-sm ?= 1.4

$border-width ?= 1px
$border-color ?= $gray-200

$border-radius ?= .25rem
$border-radius-sm ?= .2rem
$border-radius-lg ?= .3rem

// fonts
$font-size-sm ?= .875rem
$font-size-base ?= 1rem
$font-size-lg ?= 1.25rem

$font-weight-light ?= 300
$font-weight-normal ?= normal
$font-weight-bold ?= bold

$font-family-base ?= -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif
$font-family-monospace ?=  'SFMono-Regular', Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace

$h1-font-size ?= 2.5rem
$h2-font-size ?= 2rem
$h3-font-size ?= 1.75rem
$h4-font-size ?= 1.5rem
$h5-font-size ?= 1.25rem
$h6-font-size ?= $font-size-base

$headings-margin-bottom  ?= 1.5rem
$headings-font-family ?= inherit
$headings-font-weight ?= 500
$headings-line-height  ?= 1.1
$headings-color ?= inherit

// display
$display1-size ?= 6rem
$display2-size ?= 5.5rem
$display3-size ?= 4.5rem
$display4-size ?= 3.5rem

$display1-weight ?= $font-weight-light
$display2-weight ?= $font-weight-light
$display3-weight ?= $font-weight-light
$display4-weight ?= $font-weight-light

$display-line-height ?= $headings-line-height

//  This variable affects the `.h-*` and `.w-*` classes.
$size-25 ?= 25%,
$size-50 ?= 50%,
$size-75 ?= 75%,
$size-100 ?= 100%


$small-font-size ?= 80%

$lead-font-size ?= $h5-font-size
$lead-font-weight ?= $font-weight-light

$hr-border-color ?= rgba($black, .1)
$hr-border-width ?= $border-width

$blockquote-font-size ?= ($font-size-base * 1.5)

$text-muted ?= $gray-600

$null ?= 0

// helper-sizes
$helper-size-0 ?= $null
$helper-size-1 ?= .25rem
$helper-size-2 ?= .5rem
$helper-size-3 ?= 1rem
$helper-size-4 ?= 1.5rem
$helper-size-5 ?= 3rem

// text-transforms
$textTransform ?= lowercase uppercase capitalize
$text-transform-include ?= true

// directions
$directions ?= left right center
$floatDirections ?= left right none

// text-direction-sub-includes
$text-directions-base-include ?= true
$text-directions-sm-include ?= true
$text-directions-md-include ?= true
$text-directions-lg-include ?= true
$text-directions-xl-include ?= true

// text-type-sub-includes
$text-primary-include ?= true
$text-secondary-include ?= true
$text-success-include ?= true
$text-info-include ?= true
$text-warning-include ?= true
$text-danger-include ?= true
$text-light-include ?= true
$text-dark-include ?= true

// float-sub-includes
$float-base-include ?= true
$float-sm-include ?= true
$float-md-include ?= true
$float-lg-include ?= true
$float-xl-include ?= true


// text-sub-includes

@import pathjoin($basepath, 'includes')

for key, val in includes
  if val is true
    @import  pathjoin($basepath, 'includes', key)

for key, val in helpers
  if val is true
    @import  pathjoin($basepath, 'includes/helpers', key)
